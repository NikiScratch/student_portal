<p id="notice"><%= notice %></p>

<p><% @subject=Subject.all %>
  <strong>User:</strong>
  <%= @schedule.user_id %>
  <% @schedule.save %>
</p>

Add Classes to schedule<- want to make this a button that will enter class I select based on the subjects I have created<p>

<%# for subject in Subject.all %>
<div>
  <%#= check_box_tag "schedule[subject_ids][]", subject.id, @schedule.subject.include?(subject) %>
  <%#= subject.name %>
</div>
<% #end %>

<strong>All available Classes</p></strong>
<table class="table table-hover">
#Want the ability to add class
<p> 

	<% @subject.each do |subject|%>
	<%=link_to subject.name, addsubject_path(:subject_id=>subject.id, :schedule_id=> @schedule.id)%><p>
 <%end%>

</table>

<%#= simple_form_for @schedule do |f| %>
 
    <%#= f.association :subjects, :include_blank => false %>
  
  <%#= f.button :submit %>
<%# end %>

<p><strong>Classes</p></strong>

<table class="table table-hover">
<%#= schedule.user.name%>
<% @schedule.subjects.each do |subject|%>
<tr><td><%= subject.name%></td>
</tr>
<%end%>
<%#= render 'schedules/create_userschedule'%>
</table>
<%= link_to 'Edit', edit_schedule_path(@schedule) %> |
<%= link_to 'Back', schedules_path %>
